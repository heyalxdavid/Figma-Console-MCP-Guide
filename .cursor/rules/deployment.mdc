# Deployment

This repo uses GitHub Actions to deploy to GitHub Pages automatically on push to `main`.

## Workflow

The deploy workflow lives at `.github/workflows/deploy.yml`. It builds the Astro site and deploys to GitHub Pages using the official `actions/deploy-pages` action.

## After pushing to main

Pushing to `main` auto-deploys the site to GitHub Pages.
